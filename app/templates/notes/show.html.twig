{% extends 'base.html.twig' %}

{% block title %}
    {{ 'title_notes_details'|trans({'%id%': note.id|default('')}) }}
{% endblock %}

{% block body %}
    {{ include('menu.html.twig') }}
    <h2>{{ 'title_notes_details'|trans({'%id%': note.id|default('')}) }}</h2>
    {% if note is defined and note|length %}
        <dl>
            <dt>{{ 'label_id'|trans }}</dt>
            <dd>{{ note.id }}</dd>
            <dt>{{ 'label_created_at'|trans }}</dt>
            <dd>{{ note.createdAt|format_date('long') }}</dd>
            <dt>{{ 'label_updated_at'|trans }}</dt>
            <dd>{{ note.updatedAt|format_date('long') }}</dd>
            <dt>{{ 'label_tags'|trans }}</dt>
            <dd>{{  note.tags.name }}</dd>
            {% if is_granted('ROLE_ADMIN') %}
                <dt>{{ 'label_author'|trans }}</dt>
                <dd>{{  note.author.email }}</dd>
            {% endif %}
            <dt>{{ 'label_title'|trans }}</dt>
            <dd>{{  note.title }}</dd>
            <dt>{{ 'label_comment'|trans }}</dt>
            <dd>{{  note.comment }}</dd>
        </dl>
        <p>
            <a href="{{ url('notes_index') }}" title="back to the list">
                &#11013;{{ 'button_back'|trans }}
            </a>
        </p>
    {% else %}
        <p>
            {{ 'message_item_not_found'|trans }}
        </p>
    {% endif %}
{% endblock %}