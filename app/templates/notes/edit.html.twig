{% extends 'base.html.twig' %}

{% block title %}
    {{ 'title_note_edit'|trans({'%id%': note.id|default('')}) }}
{% endblock %}

{% block body %}
    {{ include('menu.html.twig') }}
    {% include 'default/_form.html.twig' with {
        page_title: 'title_note_edit',
        page_title_param: {'%id%': note.id},
        attr: {
            method: 'PUT',
            action: url('notes_edit', {'id': note.id}),
        },
        form: form,
        submit_label: 'action_edited'|trans,
        message: 'message_updated_successfully'|trans,
        back_to_list_path: 'notes_index',
    } %}
{% endblock %}